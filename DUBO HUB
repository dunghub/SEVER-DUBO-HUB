<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DUBO HUB - Secure Private Gateway</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        body {
            background: linear-gradient(135deg, #0a192f, #020c1b);
            color: #ffffff;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 30px 15px;
        }
        .container {
            width: 100%;
            max-width: 800px;
            background: rgba(10, 25, 47, 0.85);
            border: 1px solid #1e3a8a;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 0 25px rgba(0, 149, 255, 0.3);
            text-align: center;
            position: relative;
        }
        #loginScreen {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
        #loginScreen h2 {
            color: #90e0ef;
            margin-bottom: 20px;
            letter-spacing: 1px;
        }
        .login-box {
            display: flex;
            gap: 10px;
            width: 100%;
            max-width: 400px;
        }
        #mainDashboard {
            display: none;
        }
        .menu-btn {
            position: absolute;
            top: 20px;
            left: 20px;
            background: #1e3a8a;
            border: 1px solid #3b82f6;
            color: #fff;
            padding: 8px 12px;
            border-radius: 6px;
            cursor: pointer;
            font-size: 18px;
            transition: 0.2s;
            z-index: 10;
        }
        .menu-btn:hover {
            background: #2563eb;
        }
        .sidebar-menu {
            position: absolute;
            top: 0;
            left: -280px;
            width: 270px;
            height: 100%;
            background: #030712;
            border-right: 1px solid #1e3a8a;
            box-shadow: 5px 0 15px rgba(0,0,0,0.5);
            transition: 0.3s;
            z-index: 20;
            display: flex;
            flex-direction: column;
            padding-top: 60px;
            border-top-left-radius: 15px;
            border-bottom-left-radius: 15px;
            text-align: left;
        }
        .sidebar-menu.active {
            left: 0;
        }
        .menu-item {
            padding: 15px 20px;
            color: #90e0ef;
            border-bottom: 1px solid #1f2937;
            cursor: pointer;
            font-weight: 600;
            font-size: 14px;
            transition: 0.2s;
        }
        .menu-item:hover {
            background: #1e3a8a;
            color: #fff;
        }
        .avatar-box img {
            width: 110px;
            height: 110px;
            border-radius: 50%;
            border: 2px solid #00b4d8;
            box-shadow: 0 0 20px rgba(0, 180, 216, 0.6);
            margin-bottom: 15px;
            object-fit: cover;
        }
        .brand-title {
            font-size: 28px;
            font-weight: bold;
            letter-spacing: 2px;
            color: #90e0ef;
            margin-bottom: 10px;
        }
        .aura-text {
            font-size: 16px;
            color: #00f5d4;
            text-shadow: 0 0 10px rgba(0, 245, 212, 0.7);
            min-height: 30px;
            margin-bottom: 25px;
            font-family: monospace;
            border-right: 2px solid #00f5d4;
            white-space: nowrap;
            overflow: hidden;
            display: inline-block;
        }
        .section {
            background: rgba(2, 12, 27, 0.6);
            border: 1px solid #1d4ed8;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
            text-align: left;
            display: none;
        }
        .section.active-tab {
            display: block;
        }
        .section h3 {
            color: #60a5fa;
            margin-bottom: 12px;
            font-size: 18px;
        }
        .control-row {
            display: flex;
            gap: 10px;
            align-items: center;
            margin-bottom: 12px;
        }
        input[type="password"], input[type="text"] {
            flex: 1;
            padding: 10px 15px;
            background: #030712;
            border: 1px solid #3b82f6;
            border-radius: 6px;
            color: #fff;
            outline: none;
        }
        input[type="password"]:focus, input[type="text"]:focus {
            border-color: #60a5fa;
            box-shadow: 0 0 8px rgba(96, 165, 250, 0.4);
        }
        button {
            padding: 10px 20px;
            border: none;
            border-radius: 6px;
            font-weight: bold;
            cursor: pointer;
            transition: 0.2s;
        }
        .btn-primary {
            background: #2563eb;
            color: white;
        }
        .btn-primary:hover {
            background: #1d4ed8;
        }
        .btn-danger {
            background: #dc2626;
            color: white;
        }
        .btn-danger:hover {
            background: #b91c1c;
        }
        .btn-success {
            background: #10b981;
            color: white;
        }
        .btn-success:hover {
            background: #059669;
        }
        .switch-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .switch {
            position: relative;
            display: inline-block;
            width: 60px;
            height: 34px;
        }
        .switch input { 
            opacity: 0;
            width: 0;
            height: 0;
        }
        .slider {
            position: absolute;
            cursor: pointer;
            top: 0; left: 0; right: 0; bottom: 0;
            background-color: #374151;
            transition: .4s;
            border-radius: 34px;
        }
        .slider:before {
            position: absolute;
            content: "";
            height: 26px;
            width: 26px;
            left: 4px;
            bottom: 4px;
            background-color: white;
            transition: .4s;
            border-radius: 50%;
        }
        input:checked + .slider {
            background-color: #10b981;
        }
        input:checked + .slider:before {
            transform: translateX(26px);
        }
        .result-box {
            margin-top: 15px;
            background: #030712;
            padding: 12px;
            border-radius: 6px;
            border: 1px dashed #374151;
            font-size: 14px;
            max-height: 200px;
            overflow-y: auto;
        }
        .key-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 8px 0;
            border-bottom: 1px solid #1f2937;
        }
        .key-item:last-child {
            border-bottom: none;
        }
        .success-alert {
            color: #10b981;
            font-weight: bold;
            margin-top: 10px;
            display: none;
        }
        .error-msg {
            color: #ef4444;
            margin-top: 10px;
            font-size: 14px;
            display: none;
        }
    </style>
</head>
<body>

    <div class="container">
        <div id="loginScreen">
            <h2>🔒 ENTER SERVER PASSWORD</h2>
            <div class="login-box">
                <input type="password" id="passwordInput" placeholder="Enter password...">
                <button class="btn-primary" onclick="checkPassword()">Access</button>
            </div>
            <div class="error-msg" id="errorMsg">Incorrect password! Access denied.</div>
        </div>

        <div id="mainDashboard">
            <button class="menu-btn" onclick="toggleMenu()">☰</button>

            <div class="sidebar-menu" id="sidebarMenu">
                <div class="menu-item" onclick="switchTab('tabKey')">🔑 KEY PREMIUM (Vĩnh viễn)</div>
                <div class="menu-item" onclick="switchTab('tabKeyTime')">⏳ KEY TIME (Có thời hạn)</div>
                <div class="menu-item" onclick="switchTab('tabCreateKey')">➕ TẠO KEY (GENERATE)</div>
                <div class="menu-item" onclick="switchTab('tabLink')">📊 SỐ LẦN VƯỢT LINK</div>
                <div class="menu-item" onclick="switchTab('tabBypass')">🚨 SỐ LẦN BYPASS</div>
                <div class="menu-item" onclick="switchTab('tabSystem')">⚙️ SYSTEM CONTROL</div>
            </div>

            <div class="avatar-box">
                <img src="https://cdn.discordapp.com/avatars/1154388701237989386/a_f7e91d51a6652d5806e2e50d1891bca7.gif" alt="Bot Avatar">
            </div>

            <div class="brand-title">DUBO HUB</div>
            
            <div>
                <span class="aura-text" id="typedText"></span>
            </div>

            <div id="tabKey" class="section active-tab">
                <h3>KEY PREMIUM (Lifetime Keys)</h3>
                <div class="control-row">
                    <input type="text" id="searchDiscordId" placeholder="Search Discord ID or Lifetime Key...">
                    <button class="btn-primary" onclick="searchKeyByDiscord()">Search</button>
                </div>
                <div class="result-box" id="searchResult">
                    <span style="color: #6b7280;">Nhập ID Discord để tra cứu key vĩnh viễn...</span>
                </div>
            </div>

            <div id="tabKeyTime" class="section">
                <h3>KEY TIME (Timed Keys Database)</h3>
                <div class="control-row">
                    <input type="text" id="searchKeyTimeInput" placeholder="Search timed keys..." oninput="renderKeyTimeList()">
                </div>
                <div class="result-box" id="keyTimeListContainer">
                    </div>

                <h3 style="margin-top: 20px; color: #ef4444;">Secure Deletion / Deactivation</h3>
                <p style="font-size: 12px; color: #94a3b8; margin-bottom: 8px;">* Bắt buộc gõ đúng chữ yêu cầu mới được ấn Submit!</p>
                <div class="control-row">
                    <input type="text" id="secureDeleteInput" placeholder="Gõ: DELETE KEY...">
                    <input type="text" id="targetDeleteKey" placeholder="Dán mã Key cần xóa..." style="flex: 0.7;">
                    <button class="btn-danger" onclick="executeDeleteKey()">Submit</button>
                </div>
                <div class="control-row">
                    <input type="text" id="secureDisableInput" placeholder="Gõ: VOHIEUHOA KEY...">
                    <input type="text" id="targetDisableKey" placeholder="Dán mã Key cần tắt..." style="flex: 0.7;">
                    <button class="btn-danger" style="background: #d97706;" onclick="executeDisableKey()">Submit</button>
                </div>
            </div>

            <div id="tabCreateKey" class="section">
                <h3>➕ TẠO KEY MỚI</h3>
                <div class="control-row">
                    <input type="text" id="generatedKeyField" placeholder="DUBOHUB-KEY-..." readonly>
                    <button class="btn-success" onclick="generateRandomKey()">Random</button>
                </div>
                <div class="control-row">
                    <input type="text" id="keyDurationField" placeholder="Nhập thời hạn (vd: 1h, 2d, 1m, 1y hoặc ghi 'vĩnh viễn')...">
                </div>
                <div class="control-row">
                    <input type="text" id="keyDiscordIdField" placeholder="Nhập ID tài khoản Discord người mua...">
                </div>
                <button class="btn-primary" style="width: 100%; margin-top: 5px;" onclick="submitNewKey()">Submit</button>
                <div class="success-alert" id="createSuccessAlert">✅ ĐÃ TẠO KEY THÀNH CÔNG! Key đã phân loại vào hệ thống.</div>
            </div>

            <div id="tabLink" class="section">
                <h3 style="color: #10b981;">SỐ LẦN NGƯỜI DÙNG VƯỢT LINK (Successful Links)</h3>
                <div class="control-row">
                    <input type="text" id="searchLinkUser" placeholder="Search User/IP/Discord ID...">
                    <button class="btn-primary" style="background: #10b981;" onclick="searchLinkStats()">Search</button>
                </div>
                <div class="result-box" id="linkStatsResult">
                    <div class="key-item"><span>Total Successful Completes:</span> <b style="color: #10b981;">1,420 times</b></div>
                </div>
            </div>

            <div id="tabBypass" class="section">
                <h3 style="color: #ef4444;">🚨 SỐ LẦN BYPASS VƯỢT LINK (Blocked Exploiters)</h3>
                <div class="control-row">
                    <input type="text" id="searchBypassLog" placeholder="Search IP / Hardware ID / Neighbor Network...">
                    <button class="btn-danger" onclick="searchBypassStats()">Search Ban Log</button>
                </div>
                <div class="result-box" id="bypassStatsResult">
                    <div class="key-item"><span>Hardware ID: <code>MAC-88:99:AA</code></span> <span style="color: #ef4444;">Banned (12 bypass attempts)</span></div>
                </div>
            </div>

            <div id="tabSystem" class="section">
                <h3>System Main Switch</h3>
                <div class="switch-container">
                    <span id="statusText" style="color: #10b981; font-weight: bold;">System is ONLINE</span>
                    <label class="switch">
                        <input type="checkbox" id="mainSystemSwitch" checked onchange="toggleSystem()">
                        <span class="slider"></span>
                    </label>
                </div>
            </div>
        </div>
    </div>

    <script>
        const CORRECT_PASSWORD = "DUBO-HUB-04012012";

        // Tách biệt kho lưu trữ: Khóa vĩnh viễn nằm riêng, khóa có thời hạn nằm riêng
        let lifetimeKeyDatabase = [
            { key: "DUBOHUB-KEY-LIFETIME01", duration: "vĩnh viễn", discordId: "89123456789", status: "Active" }
        ];

        let timedKeyDatabase = [
            { key: "DUBOHUB-KEY-TIME01", duration: "30d", discordId: "98765432100", status: "Active" },
            { key: "DUBOHUB-KEY-TIME02", duration: "2h", discordId: "11223344556", status: "Active" }
        ];

        function checkPassword() {
            let inputPass = document.getElementById("passwordInput").value;
            let errorMsg = document.getElementById("errorMsg");
            if (inputPass === CORRECT_PASSWORD) {
                document.getElementById("loginScreen").style.display = "none";
                document.getElementById("mainDashboard").style.display = "block";
                startTypingEffect();
                renderKeyTimeList();
            } else {
                errorMsg.style.display = "block";
            }
        }

        document.getElementById("passwordInput").addEventListener("keypress", function(event) {
            if (event.key === "Enter") checkPassword();
        });

        function toggleMenu() {
            document.getElementById("sidebarMenu").classList.toggle("active");
        }

        function switchTab(tabId) {
            document.querySelectorAll('.section').forEach(t => t.classList.remove('active-tab'));
            document.getElementById(tabId).classList.add('active-tab');
            toggleMenu();
            if(tabId === 'tabKeyTime') renderKeyTimeList();
        }

        const textToType = "CHÀO MỪNG ĐẾN VỚI SEVER DUBO HUB";
        let charIndex = 0;
        function startTypingEffect() {
            function type() {
                if (charIndex < textToType.length) {
                    document.getElementById("typedText").innerHTML += textToType.charAt(charIndex);
                    charIndex++;
                    setTimeout(type, 100);
                }
            }
            type();
        }

        function generateRandomKey() {
            let randomPart = Math.random().toString(36).substring(2, 8).toUpperCase() + Math.floor(Math.random() * 900 + 100);
            document.getElementById("generatedKeyField").value = "DUBOHUB-KEY-" + randomPart;
        }

        // Tự động phân loại: nếu hạn là "vĩnh viễn" (hoặc không nhập thời hạn) -> vào KEY PREMIUM, còn lại vào KEY TIME
        function submitNewKey() {
            let keyValue = document.getElementById("generatedKeyField").value.trim();
            let durationValue = document.getElementById("keyDurationField").value.trim().toLowerCase();
            let discordIdValue = document.getElementById("keyDiscordIdField").value.trim();

            if (!keyValue || !durationValue || !discordIdValue) {
                alert("Vui lòng điền đầy đủ: Random Key, Thời hạn và ID Discord!");
                return;
            }

            let newItem = {
                key: keyValue,
                duration: durationValue,
                discordId: discordIdValue,
                status: "Active"
            };

            if (durationValue === "vĩnh viễn" || durationValue === "lifetime" || durationValue === "vn") {
                lifetimeKeyDatabase.push(newItem);
            } else {
                timedKeyDatabase.push(newItem);
            }

            let alertBox = document.getElementById("createSuccessAlert");
            alertBox.style.display = "block";
            setTimeout(() => { alertBox.style.display = "none"; }, 4000);

            document.getElementById("generatedKeyField").value = "";
            document.getElementById("keyDurationField").value = "";
            document.getElementById("keyDiscordIdField").value = "";
            renderKeyTimeList();
        }

        function renderKeyTimeList() {
            let container = document.getElementById("keyTimeListContainer");
            let filterText = document.getElementById("searchKeyTimeInput") ? document.getElementById("searchKeyTimeInput").value.trim().toLowerCase() : "";
            
            let html = "";
            let filtered = timedKeyDatabase.filter(item => item.key.toLowerCase().includes(filterText) || item.discordId.includes(filterText));

            if (filtered.length === 0) {
                container.innerHTML = "<span style='color: #6b7280;'>Không có key có thời hạn nào trong hệ thống.</span>";
                return;
            }

            filtered.forEach(item => {
                let statusColor = item.status === "Active" ? "#10b981" : "#ef4444";
                html += `<div class="key-item">
                    <div><b>${item.key}</b> <span style="color: #94a3b8; font-size:12px;">(Hạn: ${item.duration} | Discord: ${item.discordId})</span></div>
                    <span style="color: ${statusColor}; font-weight: bold;">[${item.status}]</span>
                </div>`;
            });
            container.innerHTML = html;
        }

        function searchKeyByDiscord() {
            let inputId = document.getElementById("searchDiscordId").value.trim().toLowerCase();
            let resultBox = document.getElementById("searchResult");
            if(!inputId) {
                resultBox.innerHTML = "<span style='color: #ef4444;'>Vui lòng nhập ID Discord hoặc Key vĩnh viễn!</span>";
                return;
            }
            let founds = lifetimeKeyDatabase.filter(item => item.discordId.toLowerCase().includes(inputId) || item.key.toLowerCase().includes(inputId));
            if(founds.length > 0) {
                let html = `<b>Kết quả Key vĩnh viễn:</b><br>`;
                founds.forEach(f => {
                    html += `<div class="key-item"><span>Key: <code>${f.key}</code> (Vĩnh viễn)</span><span style="color: #10b981;">[${f.status}] - Discord: ${f.discordId}</span></div>`;
                });
                resultBox.innerHTML = html;
            } else {
                resultBox.innerHTML = "<span style='color: #f59e0b;'>Không tìm thấy key vĩnh viễn cho ID này.</span>";
            }
        }

        // Xóa Key an toàn (Hỗ trợ tìm xóa ở cả 2 kho)
        function executeDeleteKey() {
            let confirmText = document.getElementById("secureDeleteInput").value.trim();
            let keyToDel = document.getElementById("targetDeleteKey").value.trim();

            if (confirmText !== "DELETE KEY") {
                alert("LỖI: Bạn phải gõ chính xác chữ 'DELETE KEY' để xác nhận!");
                return;
            }
            if (!keyToDel) {
                alert("Vui lòng dán mã key cần xóa!");
                return;
            }

            let indexLife = lifetimeKeyDatabase.findIndex(item => item.key === keyToDel);
            let indexTime = timedKeyDatabase.findIndex(item => item.key === keyToDel);

            if (indexLife !== -1) {
                lifetimeKeyDatabase.splice(indexLife, 1);
                alert("THÀNH CÔNG: Đã xóa vĩnh viễn key premium: [" + keyToDel + "]");
            } else if (indexTime !== -1) {
                timedKeyDatabase.splice(indexTime, 1);
                alert("THÀNH CÔNG: Đã xóa key time: [" + keyToDel + "]");
                renderKeyTimeList();
            } else {
                alert("Không tìm thấy key này trong hệ thống!");
                return;
            }

            document.getElementById("secureDeleteInput").value = "";
            document.getElementById("targetDeleteKey").value = "";
        }

        // Vô hiệu hóa Key an toàn (Hỗ trợ tìm tắt ở cả 2 kho)
        function executeDisableKey() {
            let confirmText = document.getElementById("secureDisableInput").value.trim();
            let keyToDis = document.getElementById("targetDisableKey").value.trim();

            if (confirmText !== "VOHIEUHOA KEY") {
                alert("LỖI: Bạn phải gõ chính xác chữ 'VOHIEUHOA KEY' để xác nhận!");
                return;
            }
            if (!keyToDis) {
                alert("Vui lòng dán mã key cần vô hiệu hóa!");
                return;
            }

            let found = lifetimeKeyDatabase.find(item => item.key === keyToDis) || timedKeyDatabase.find(item => item.key === keyToDis);
            if (found) {
                found.status = "Deactivated";
                alert("THÀNH CÔNG: Key [" + keyToDis + "] đã bị VÔ HIỆU HÓA!");
                document.getElementById("secureDisableInput").value = "";
                document.getElementById("targetDisableKey").value = "";
                renderKeyTimeList();
            } else {
                alert("Không tìm thấy key này trong hệ thống!");
            }
        }

        function toggleSystem() {
            let isChecked = document.getElementById("mainSystemSwitch").checked;
            let statusText = document.getElementById("statusText");
            if(isChecked) {
                statusText.innerText = "System is ONLINE";
                statusText.style.color = "#10b981";
            } else {
                statusText.innerText = "System is OFFLINE (Locked)";
                statusText.style.color = "#ef4444";
            }
        }

        function searchLinkStats() { alert("Đang quét thống kê lượt vượt link..."); }
        function searchBypassStats() { alert("Đang quét danh sách blacklist bypass..."); }
    </script>
</body>
</html>
